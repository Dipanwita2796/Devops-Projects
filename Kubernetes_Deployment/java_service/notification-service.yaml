apiVersion: v1
kind: Service
metadata:
  name: notification-service
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8099
    targetPort: 8099
    protocol: TCP
  selector:
    app: notification-service
