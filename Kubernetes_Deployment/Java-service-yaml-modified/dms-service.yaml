apiVersion: apps/v1
kind: Deployment
metadata:
  name: dms-service
  labels:
    app: dms-service
spec:
  selector:
    matchLabels:
      app: dms-service
  replicas: 1
  template:
    metadata:
      labels:
        app: dms-service
    spec:
      containers:
      - name: dms-service
        image: 083887970427.dkr.ecr.ap-south-1.amazonaws.com/test:dms-service_ubuntu_v0-61-0_10082023
        imagePullPolicy: IfNotPresent
        readinessProbe:
          tcpSocket:
            port: 8087
          initialDelaySeconds: 60
          periodSeconds: 20
          timeoutSeconds: 30
          failureThreshold: 10
          successThreshold: 1
        ports:
        - containerPort: 8087
        envFrom:
          - configMapRef:
              name: java-env-configmap
        resources:
          requests:
            cpu: 500m
            memory: "1000Mi"
          limits:
            cpu: 500m
            memory: "1000Mi"
      nodeSelector:
        eks.amazonaws.com/nodegroup: xlrt-java-app
    
---
apiVersion: v1
kind: Service
metadata:
  name: dms-service
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8087
    targetPort: 8087
    protocol: TCP
  selector:
    app: dms-service
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: dms-service
  labels:
    version: v1
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: dms-service
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
---


