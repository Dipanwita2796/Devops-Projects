apiVersion: apps/v1
kind: Deployment
metadata:
  name: docparser-gateway-service
  labels:
    app: docparser-gateway-service
  
spec:
  selector:
    matchLabels:
      app: docparser-gateway-service
  replicas: 1
  template:
    metadata:
      labels:
        app: docparser-gateway-service
    spec:
      containers:
      - name: docparser-gateway-service
        image: 083887970427.dkr.ecr.ap-south-1.amazonaws.com/test:docparser-gateway-service_ubuntu_v0-61-0_10082023
        imagePullPolicy: IfNotPresent
        readinessProbe:
          tcpSocket:
            port: 8070
          initialDelaySeconds: 60
          periodSeconds: 20
          timeoutSeconds: 30
          failureThreshold: 10
          successThreshold: 1
        ports:
        - containerPort: 8070
        envFrom:
          - configMapRef:
              name: java-env-configmap
        resources:
          requests:
            cpu: 500m
            memory: "1000Mi"
          limits:
            cpu: 500m
            memory: "1000Mi"
      nodeSelector:
        eks.amazonaws.com/nodegroup: xlrt-java-app
---
apiVersion: v1
kind: Service
metadata:
  name: docparser-gateway-service
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8070
    targetPort: 8070
    protocol: TCP
  selector:
    app: docparser-gateway-service
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: docparser-gateway-service
  labels:
    version: v1
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: docparser-gateway-service
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
---


