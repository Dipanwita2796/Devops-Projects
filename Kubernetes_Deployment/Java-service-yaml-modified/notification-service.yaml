apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  labels:
    app: notification-service
spec:
  selector:
    matchLabels:
      app: notification-service
  replicas: 1
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
      - name: notification-service
        image: 083887970427.dkr.ecr.ap-south-1.amazonaws.com/test:notification-service_ubuntu_v0-61-0_10082023
        imagePullPolicy: IfNotPresent
        readinessProbe:
          tcpSocket:
            port: 8099
          initialDelaySeconds: 60
          periodSeconds: 20
          timeoutSeconds: 30
          failureThreshold: 10
          successThreshold: 1
        ports:
        - containerPort: 8099
        envFrom:
          - configMapRef:
              name: java-env-configmap
        resources:
          requests:
            cpu: 250m
            memory: "500Mi"
          limits:
            cpu: 250m
            memory: "500Mi"
      nodeSelector:
        eks.amazonaws.com/nodegroup: xlrt-java-app
---
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8099
    targetPort: 8099
    protocol: TCP
  selector:
    app: notification-service
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: notification-service
  labels:
    version: v1
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: notification-service
  minReplicas: 1
  maxReplicas: 70
  targetCPUUtilizationPercentage: 70
---


