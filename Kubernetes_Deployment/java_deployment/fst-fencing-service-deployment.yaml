apiVersion: apps/v1
kind: Deployment
metadata:
  name: fst-fencing-service
spec:
  selector:
    matchLabels:
      app: fst-fencing-service
  replicas: 1
  template:
    metadata:
      labels:
        app: fst-fencing-service
    spec:
      containers:
      - name: fst-fencing-service
        image: 647223405008.dkr.ecr.me-central-1.amazonaws.com/striderisk-delivery:fst-fencing-service_ubuntu_v0-52-0_20042023_dnb_live1
        imagePullPolicy: IfNotPresent
        #volumeMounts:
         # - name: fst-service-logs
          #  mountPath: /var/log/java/fst-service:/logs
        #livenessProbe:
         # exec:
          #  command:
           #  - cat
            # - /fst-service/pom.xml
          #initialDelaySeconds: 60
          #periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: 8110
          initialDelaySeconds: 60
          periodSeconds: 20
          timeoutSeconds: 30
          failureThreshold: 10
          successThreshold: 1
        ports:
        - containerPort: 8110
        envFrom:
          - configMapRef:
              name: java-env-configmap
        resources:
          requests:
            cpu: 500m
            memory: "1000Mi"
          limits:
            cpu: 500m
            memory: "1000Mi"
