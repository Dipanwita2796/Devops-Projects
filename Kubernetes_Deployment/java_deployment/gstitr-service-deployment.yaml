apiVersion: apps/v1
kind: Deployment
metadata:
  name: gstitr-processor-service
spec:
  selector:
    matchLabels:
      app: gstitr-processor-service
  replicas: 1
  template:
    metadata:
      labels:
        app: gstitr-processor-service
    spec:
      containers:
      - name: gstitr-processor-service
        image: 647223405008.dkr.ecr.me-central-1.amazonaws.com/striderisk-delivery:gstitr-processor_ubuntu_v0-52-0_20042023_dnb_live1
        imagePullPolicy: IfNotPresent
        #volumeMounts:
         # - name: template-management-service-logs
          #  mountPath: 
           #     -/var/log/java/template:/logs
        #livenessProbe:
         # exec:
          #  command:
           #  - cat
            # - /template-management-service/pom.xml
          #initialDelaySeconds: 60
          #periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: 8076
          initialDelaySeconds: 60
          periodSeconds: 20
          timeoutSeconds: 30
          failureThreshold: 10
          successThreshold: 1
        ports:
        - containerPort: 8076
        envFrom:
          - configMapRef:
              name: java-env-configmap
        resources:
          requests:
            cpu: 500m
            memory: "1000Mi"
          limits:
            cpu: 500m
            memory: "1000Mi"
